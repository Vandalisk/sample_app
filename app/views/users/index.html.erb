<% provide(:title, 'All Users') %>
<h1>Users</h1>
<% if @users.length == 0 %>
  <p>List of users is empty</p>
<% else %>
  <table>
	<% @users.each do |user| %>
	  <tr>
	   <td><%= user.name %></td>
	   <td><%= user.email %></td>
	  </tr>
   <% end %>
  </table>
<% end %>
