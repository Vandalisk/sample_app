<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate %>

<ul class="users">
  <% if @users.length == 0 %>
    <p>List of users is empty</p>
  <% else %>
    <%= render @users %>
  <% end %>
</ul>

<%= will_paginate %>
